# TODO: Replace Dummy Map with SVG in NorthCampus.jsx

## Tasks

- [ ] Update BUILDINGS array with all buildings from SVG (names, x, y from text, width/height 100, colors)
- [ ] Inline the new SVG from assets into NorthCampus.jsx, replacing the dummy SVG
- [ ] Add onClick to <g id="Buildings"> for clickability on building paths (except faculty area)
- [ ] Adjust SVG scaling to fit 1000x600 while preserving aspect ratio
- [ ] Test zoom and search functionality with new buildings
